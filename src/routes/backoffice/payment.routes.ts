/**
 * @file payment.routes.ts
 * @description Backoffice payment obligation routes (slip upload removed — use Enduser submit + Backoffice approve)
 */

import { Router } from 'express'

const router = Router()

export default router
